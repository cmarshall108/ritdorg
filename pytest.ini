[pytest]
# Test modules in this repo are named tests_*.py (not the pytest default
# test_*.py / *_test.py), and are written against unittest.TestCase so they
# can also be run via `python -m unittest tests.tests_core_api`. This makes
# `pytest` discover them too.
python_files = tests_*.py test_*.py
testpaths = tests
